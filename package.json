{
  "name": "mxmap",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build-release.js",
    "prepare:release": "node scripts/build-release.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "leaflet": "^1.9.4"
  }
}
